name: End of Day Report

on:
  workflow_run:
    workflows: ["Intraday Scan"]
    types:
      - completed
  workflow_dispatch:

jobs:
  eod:
    runs-on: ubuntu-latest

    steps:
      # -----------------------------
      # 1️⃣ Checkout repository
      # -----------------------------
      - name: Checkout repo
        uses: actions/checkout@v4

      # -----------------------------
      # 2️⃣ Setup Python
      # -----------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # -----------------------------
      # 3️⃣ Install dependencies
      # -----------------------------
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # -----------------------------
      # 4️⃣ Wait for artifact availability (CRITICAL)
      # -----------------------------
      - name: Wait for intraday artifact
        run: sleep 37

      # -----------------------------
      # 5️⃣ Download intraday signals
      # -----------------------------
      - name: Download intraday signals
        uses: actions/download-artifact@v4
        with:
          name: intraday-signals
          path: data
          run-id: ${{ github.event.workflow_run.id }}

      # -----------------------------
      # 6️⃣ Run EOD report
      # -----------------------------
      - name: Run EOD report
        run: python Scripts/eod_report.py
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
